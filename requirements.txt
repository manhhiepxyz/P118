# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9   # upload ảnh giấy tờ xác thực (PATCH /users/me, verification-records)
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# Google Auth
google-auth>=2.23.0
google-auth-httplib2>=0.1.1

# Database
asyncpg>=0.29.0          # async PostgreSQL driver (dùng trực tiếp, không qua SQLAlchemy)
sqlalchemy>=2.0.0      # bỏ comment nếu muốn dùng ORM sau này
alembic>=1.14.0        # bỏ comment nếu muốn migration tool

# Vector Store (uncomment as needed)
# chromadb>=0.5.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0

# Email
aiosmtplib>=3.0.1

